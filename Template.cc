/* $Id: Template.cc,v 1.1 2012/02/03 13:17:10 akadams Exp $ */

// Copyright © 2010, Pittsburgh Supercomputing Center (PSC).  
// See the file 'COPYRIGHT.txt' for any restrictions.

// XXX Any includes not in Template.h in /usr/include/sys first (in alphabetical order), e.g.: #include <sys/types.h>

// XXX a space, then any network includes not in Template.h next (in alphabetical order), e.g.: #include <netinet/tcp.h>

// XXX a space, then includes in /usr/include not in Template.h (in alphabetical order), e.g. : #include <err.h>\n#include <stdint.h>

// XXX a space, STL includes not in Template.h next.
#include <string>
using namespace std;

// XXX A space, and any local header files includes needed that are not in Template.h.

#include "Template.h"

// Non-class specific defines & data structures.

// Non-class specific utility functions.

// Template Class.

// Constructors and destructor.
Template::Template(void) {
}

Template::~Template(void) {
  // For now, do nothing.
}

// Copy constructor, assignment and equality operator, needed for STL.

// Accessors.

// Mutators.

// Template manipulation.

// Boolean checks.


// XXX Two spaces between classes within .cc file.
